# Shared client-safe utilities

Planned content: accessible UI primitives, formatting, pagination DTO helpers and generic non-secret utilities. Domain logic belongs in modules. No database connections, model SDKs, environment secrets or server re-exports.

Only add utilities with actual shared use during implementation. See [module boundaries](../../docs/MODULE_STRUCTURE.md).
